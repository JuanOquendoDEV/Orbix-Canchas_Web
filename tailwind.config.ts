import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      colors: {
        background: "#008c9a",
        primary: "#FFD700",
        secondary: "#87AECE",
        accent: "#004f55",
      },
    },
  },
  plugins: [],
}

export default config
